name: Release and Deploy (HTML Simples)

on:
  push:
    tags:
      - 'v*.*.*' # Exemplo: v1.0.0, v2.1.3 etc.

jobs:
  deploy:
    name: Publicar versão estática na Vercel
    runs-on: ubuntu-latest

    steps:
      - name: Checkout do repositório
        uses: actions/checkout@v3

      - name: Mostrar versão publicada
        run: echo "🚀 Publicando versão ${GITHUB_REF_NAME}"

      # Deploy para Vercel (projeto estático)
      - name: Deploy para Vercel
        uses: amondnet/vercel-action@v25
        with:
          vercel-token: ${{ secrets.VERCEL_TOKEN }}
          vercel-org-id: ${{ secrets.VERCEL_ORG_ID }}
          vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID }}
          working-directory: ./ # pasta onde está seu index.html
          vercel-args: '--prod'
